const lighthouses = ["Gibraltar Point", "Peggy's Point", "Cove Island",
  "Discovery Island", "Cape Scott", "Point Clark", "Kincardine"];

const wordLengths = lighthouses.map(word => word.length);

console.log(wordLengths);